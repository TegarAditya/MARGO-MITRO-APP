services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: laravel-app:latest
    container_name: margo_mitro_old
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${APP_PORT:-9001}:9000"
    command: ["php-fpm"]
